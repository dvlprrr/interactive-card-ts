type PropType = {
  handleCardNumber: (value: string) => void;
  handleCardName: (value: string) => void;
};
export default function CardForm({
  handleCardNumber,
  handleCardName,
}: PropType) {
  return (
    <div>
      <div></div>
    </div>
  );
}
